#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Validate checkout files') {
            steps {
                sh """
                pwd
                ls -lart
                date
                """
            }
        }
    }
}